package reema.com.producer;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.jms.core.JmsTemplate;
import org.springframework.stereotype.Component;

//import reema.com.model.Employee;
import reema.com.model.Person;

//not used
//@Component
public class JmsProducer {

	/*
	 * //@Autowired private JmsTemplate jmsTemplate;
	 * 
	 * 
	 * private final XmlMessageConverter messageConverter;
	 * 
	 * 
	 * 
	 * public JmsProducer(JmsTemplate jmsTemplate, XmlMessageConverter
	 * messageConverter) { this.jmsTemplate = jmsTemplate; this.messageConverter =
	 * messageConverter; this.jmsTemplate.setMessageConverter(messageConverter); }
	 * 
	 * 
	 * public void sendMessage(String destination, Person person) {
	 * jmsTemplate.convertAndSend(destination, person);
	 * 
	 * }
	 */
    
    
}
